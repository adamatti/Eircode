web:
  build: .
  command: ./gradlew run
  ports:
    - "4567:4567"
  expose:
    - "4567"
  environment:
    PORT: "4567"
    FAKE_ENABLED: "true"

tests:
  build: .
  command: ./gradlew e2eTest
  links:
    - web
  environment:
    SERVER_TO_TEST: "web"
    PORT: "4567"

